{
  "name": "@origamitower/lingua",
  "version": "0.3.0",
  "bin": {
    "lingua": "lingua-app.js"
  },
  "scripts": {
    "build": "dotnet fable lingua.fsproj --outDir build --sourceMaps && webpack",
    "build-grammar": "fohm compile grammar/grammar.ohm --name Lingua --outDir source/generated"
  },
  "dependencies": {
    "ohm-js": "^0.14.0"
  },
  "devDependencies": {
    "@origamitower/fohm": "^1.1.0",
    "webpack-cli": "^4.5.0"
  }
}
